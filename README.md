
# How to tag in git:
git tag -a task-003-complete -m "TASK-003 completed"
git push origin task-003-complete

# to build your app:
#to compile: cmake -S . -B ./build
#to build:   cmake --build build

# Start application:
./build/devicehub 